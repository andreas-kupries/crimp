crimp_primitive random_uniform {
    int width 
    int height 
} image {
    crimp_image* result;
    int          x, y;

    result = crimp_new_float (width, height);

    for (y = 0; y < crimp_h (result); y++) {
        for (x = 0; x < crimp_w (result); x++) {
	    FLOATP (result, x, y) =  RAND_FLOAT();
        }
    }

    return result;

}

# - -- --- ----- -------- -------------
# vim: set sts=4 sw=4 tw=80 et ft=tcl:
#
# Local Variables:
# mode: tcl
# fill-column: 78
# End:
#
