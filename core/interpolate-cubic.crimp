crimp_primitive interpolate::cubic {
    double a
    double b
    double c
    double d
    double xf
} double {
    /* TODO: See if we can support a syntax which simply assigns the function */
    return crimp_interpolate_cubic (a, b, c, d, xf);
}

# - -- --- ----- -------- -------------
# vim: set sts=4 sw=4 tw=80 et ft=tcl:
#
# Local Variables:
# mode: tcl
# fill-column: 78
# End:
#
