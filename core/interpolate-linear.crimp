crimp_primitive interpolate::linear {
    double a
    double b
    double xf
} double {
    /* TODO: See if we can support a syntax which simply assigns the function */
    return crimp_interpolate_linear (a, b, xf);
}

# - -- --- ----- -------- -------------
# vim: set sts=4 sw=4 tw=80 et ft=tcl:
#
# Local Variables:
# mode: tcl
# fill-column: 78
# End:
#
