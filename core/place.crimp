crimp_primitive place {
    image image
    int   x
    int   y
} image {
    image = crimp_dup (image);
    crimp_place (image, x, y);
    return image;
}

# - -- --- ----- -------- -------------
# vim: set sts=4 sw=4 tw=80 et ft=tcl:
#
# Local Variables:
# mode: tcl
# fill-column: 78
# End:
#
