crimp_primitive thresholdg_inside_double {
    image_double image
    double min
    double max
} image {
    crimp_image* result;
    int          x, y;

    result = crimp_new_like (image);

    for (y = 0; y < crimp_h (image); y++) {
        for (x = 0; x < crimp_w (image); x++) {

	    DOUBLEP (result, x, y) = 
	        (min <= DOUBLEP (image, x, y)) &&
	        (DOUBLEP (image, x, y) <= max)
	        ? 1.0
	        : 0.0
	        ;
        }
    }

    return result;
}

# - -- --- ----- -------- -------------
# vim: set sts=4 sw=4 tw=80 et ft=tcl:
#
# Local Variables:
# mode: tcl
# fill-column: 78
# End:
#
