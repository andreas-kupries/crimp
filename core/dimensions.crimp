crimp_primitive dimensions {
    image image
} object0 {
    Tcl_Obj* list [2];

    list [0] = Tcl_NewIntObj (crimp_w (image));
    list [1] = Tcl_NewIntObj (crimp_h (image));

    return Tcl_NewListObj (2, list);
}

# - -- --- ----- -------- -------------
# vim: set sts=4 sw=4 tw=80 et ft=tcl:
#
# Local Variables:
# mode: tcl
# fill-column: 78
# End:
#
