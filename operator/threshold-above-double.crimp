crimp_primitive thresholdg_above_double {
    image_double image
    double threshold
} image {
    crimp_image* result;
    int          x, y;

    result = crimp_new_like (image);

    for (y = 0; y < crimp_h (image); y++) {
        for (x = 0; x < crimp_w (image); x++) {

	    DOUBLEP (result, x, y) = 
	        DOUBLEP (image, x, y) > threshold 
	        ? 1.0
	        : 0.0
	        ;
        }
    }

    return result;
}

# - -- --- ----- -------- -------------
# vim: set sts=4 sw=4 tw=80 et ft=tcl:
#
# Local Variables:
# mode: tcl
# fill-column: 78
# End:
#
