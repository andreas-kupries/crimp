crimp_primitive rplace {
    image image
    int dx
    int dy
} image {
    image = crimp_dup (image);
    crimp_place (image, crimp_x(image) + dx,
	                crimp_y(image) + dy);

    return image;
}

# - -- --- ----- -------- -------------
# vim: set sts=4 sw=4 tw=80 et ft=tcl:
#
# Local Variables:
# mode: tcl
# fill-column: 78
# End:
#
